# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
*.code-workspace

# Source images (kept locally, but converted to .webp for the site)
/assets/images/*.png
/assets/images/*.jpg
/assets/images/*.jpeg

# Internal business files (never publish prices/costs)
*.xlsx
*.xlsx.tmp
*.tmp
~$*

# Local project-memory / handoff notes for Claude (never publish)
CLAUDE.md
